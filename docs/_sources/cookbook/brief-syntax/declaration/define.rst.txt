``define``
==========

A global binding can be declared with the following syntax::

    define some-name = Atomic-expression;

It may be preceded by some documentation comment with the syntax::

    /// Some documentation comment
    /// with perhaps multiple lines
    define some-name = Atomic-expression;


Example
-------

::

    define pi = 3.14;