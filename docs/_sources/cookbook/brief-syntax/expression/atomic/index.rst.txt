Atomic expressions
==================

An atomic expression is an expression that requires a known
amount of work to be executed, and can never fail. Atomic
expressions are the only kind of expressions allowed in
a ``define`` declaration.

.. toctree::

   boolean
   float
   global
   integer
   interpolate
   lazy
   list
   new
   nothing
   record
   text