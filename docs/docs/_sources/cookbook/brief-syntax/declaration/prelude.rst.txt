``prelude``
===========

Prelude declarations schedule some computation to run before the
application is executed. It has the following syntax::

    prelude
      Expression;
      Goes;
      Here;
    end

It does not accept any documentation comment preceding it.