Complex expressions
===================

A complex expression is something that Crochet needs to perform
an unknown amount of work to execute---and executing it may
still fail. Complex expressions are allowed everywhere expressions
are, but they are not allowed in ``define`` declarations.

.. toctree::

   apply
   condition
   continue-with
   delayed
   do
   for
   force
   foreign
   handle
   hole
   invocation
   perform
   pipe
   projection
   return
   self-expr
   static-type
   variable