``self`` expression
===================

The self expression refers to the self parameter of a command. It is only
usable in Postfix and Keyword commands, and will refer to the first parameter
on each of these. It has the following syntax::

    self