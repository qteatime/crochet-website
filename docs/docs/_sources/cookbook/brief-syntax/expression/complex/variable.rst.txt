Variable references
===================

A local binding (variable) can be referenced by its name. It has the following
syntax::

    Some-variable

Names have to start with an upper-case latin letter, and can be followed by
any number of latin letters, digits (``0`` to ``9``), or hyphens (``-``).