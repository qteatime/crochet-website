Static types
============

A static type is an object representation of the type itself. It's used
whenever the type has to act as a regular value (e.g.: for ``_ is Type`` tests
or ``_ as Type`` casts).

They have the following syntax::

    #type-name
